Write one Xiaohongshu image/text post in the knowledge style, using only the supplied canonical JSON as factual authority.

Return exactly one JSON object with exactly these keys: schema_version, style, title, body, tags, images. Set schema_version to 1 and style to "knowledge". Copy all image paths exactly once; their order may be chosen intentionally. Use non-empty string arrays for tags and images.

Make the post denser and clearly organized. Expose relationships already supported by canonical and group source-supported points for useful reading, while staying natural rather than academic or corporate. Preserve source language and useful original wording. Do not add any unsupported experience, date, number, statistic, achievement, expertise, opinion, event, conclusion, or image claim. Avoid generic introductions/conclusions, clickbait, excessive symmetry/headings/emoji, and formulaic transitions. Output JSON only.
